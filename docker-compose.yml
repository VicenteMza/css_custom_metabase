version: "3.8"

services:
  metabase:
    image: metabase/metabase:latest
    ports:
      - "3000:3000"

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./custom:/var/www/custom
